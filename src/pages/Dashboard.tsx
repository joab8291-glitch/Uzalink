import {useEffect,useState} from "react";import {Container,btnClass} from "@/components/ui";import {Icon,Logo} from "@/components/Icon";import {api} from "@/lib/api";import {useAuth} from "@/lib/auth";import {navigate} from "@/lib/router";
export function Dashboard(){const {user,loading,logout}=useAuth();const [data,setData]=useState<any>();const [error,setError]=useState("");const [payout,setPayout]=useState("");const [message,setMessage]=useState("");useEffect(()=>{if(!loading&&(!user||!['SELLER','ADMIN'].includes(user.role))){navigate('/seller-login');return}if(user)api.sellerDashboard().then(setData).catch(e=>setError(e.message))},[loading,user]);if(loading||!user)return <div className="min-h-screen pt-32 text-center">Loading seller account…</div>;const seller=data?.seller;const orders=data?.orders||[];return <section className="min-h-screen bg-mint/40 pb-20 pt-24"><Container className="max-w-7xl"><div className="flex flex-wrap items-center justify-between gap-4 rounded-3xl bg-white p-5 border border-forest/10"><div className="flex items-center gap-3"><Logo/><div><p className="font-extrabold text-deep">{user.name}</p><p className="text-xs text-forest/55">{seller?.handle||'Seller account'}</p></div></div><div className="flex gap-2"><button onClick={()=>navigate('/sell')} className={btnClass('gold','md')}>New product</button><button onClick={async()=>{await logout();navigate('/')}} className={btnClass('outline','md')}>Log out</button></div></div>{error&&<div className="mt-5 rounded-2xl bg-red-50 p-4 text-red-700">{error}</div>}<div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Stat label="Available balance" value={`KSh ${((seller?.balanceCents||0)/100).toLocaleString()}`} icon="wallet"/><Stat label="Pending payouts" value={`KSh ${((seller?.pendingCents||0)/100).toLocaleString()}`} icon="clock"/><Stat label="Lifetime seller net" value={`KSh ${((seller?.lifetimeSalesCents||0)/100).toLocaleString()}`} icon="chart"/><Stat label="Orders" value={seller?.totalOrders||0} icon="bag"/></div><div className="mt-6 grid gap-6 lg:grid-cols-[1.4fr_.6fr]"><div className="rounded-3xl bg-white p-6 border border-forest/10"><div className="flex items-center justify-between"><h2 className="text-xl text-deep">Your orders</h2><span className="text-xs font-bold text-forest/50">5% platform commission</span></div><div className="mt-4 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b"><th className="p-3">Order</th><th className="p-3">Product</th><th className="p-3">Gross</th><th className="p-3">Your 95%</th><th className="p-3">Status</th></tr></thead><tbody>{orders.map((o:any)=>{const i=o.items?.[0];return <tr key={o.id} className="border-b border-forest/5"><td className="p-3 font-bold">{o.publicId}</td><td className="p-3">{i?.product?.name||'—'}</td><td className="p-3">KSh {(o.amountCents/100).toLocaleString()}</td><td className="p-3 font-extrabold">KSh {(o.sellerNetCents/100).toLocaleString()}</td><td className="p-3">{o.status}</td></tr>})}</tbody></table>{!orders.length&&<p className="py-10 text-center text-forest/50">No orders yet.</p>}</div></div><div className="rounded-3xl bg-white p-6 border border-forest/10"><h2 className="text-xl text-deep">Request payout</h2><p className="mt-2 text-sm text-forest/65">Funds become available after payment confirmation.</p><input value={payout} onChange={e=>setPayout(e.target.value.replace(/\D/g,''))} placeholder="Amount in KSh" className="mt-5 w-full rounded-2xl border border-forest/10 p-3"/><button onClick={async()=>{setMessage('');try{await api.payout(Math.round(Number(payout)*100));setMessage('Payout request created.');setPayout('');const d=await api.sellerDashboard();setData(d)}catch(e){setMessage(e instanceof Error?e.message:'Payout failed')}}} className={btnClass('deep','lg','mt-3 w-full')}>Request payout</button>{message&&<p className="mt-3 text-sm font-semibold text-brand">{message}</p>}<div className="mt-7 border-t pt-5"><p className="text-xs font-bold uppercase tracking-wide text-forest/50">Settlement number</p><p className="mt-2 font-extrabold text-deep">{seller?.paymentNumber||'Not set'}</p><p className="mt-2 text-xs text-forest/55">Changing settlement details requires server-side verification.</p></div></div></div></Container></section>}
function Stat({label,value,icon}:{label:string;value:any;icon:string}){return <div className="rounded-3xl bg-white p-5 border border-forest/10"><span className="flex h-10 w-10 items-center justify-center rounded-2xl bg-mint text-brand"><Icon name={icon} className="h-5 w-5"/></span><p className="mt-4 text-xs font-bold uppercase tracking-wide text-forest/50">{label}</p><p className="mt-1 text-2xl font-extrabold text-deep">{value}</p></div>}
